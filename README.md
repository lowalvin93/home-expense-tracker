# Home Expense Tracker

A simple, phone-friendly Streamlit app for recording household expenses and receipt photos.

## Run locally

```powershell
python -m pip install -r requirements.txt
streamlit run app.py
```

Expense records are stored in `data/expenses.csv`. Uploaded JPG and PNG receipt images are stored in `receipts/`.

